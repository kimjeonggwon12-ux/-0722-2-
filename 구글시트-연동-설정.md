# 시험 신청 → 구글 스프레드시트 연동 설정 방법

`index.html`의 "시험 신청" 화면은 이미 완성되어 있고, 지금은 신청 내역이 이 브라우저(기기)의 로컬 저장소에만 임시 저장됩니다.
아래 순서대로 진행하면 신청/취소 내역이 구글 스프레드시트에 실시간으로 쌓이고, 나중에 대시보드로 이어서 볼 수 있습니다.

## 1. 구글 스프레드시트 만들기
1. 새 구글 스프레드시트를 하나 만듭니다. (이름 예: `시온 공부방 시험 신청`)
2. 시트 탭 이름은 그대로 두거나 원하는 이름으로 바꿔도 됩니다. 단, 아래 2번 코드의 `SHEET_NAME` 값과 일치시켜 주세요. (기본값: `시험신청`)

## 2. Apps Script 붙여넣기
1. 스프레드시트 메뉴에서 **확장 프로그램 → Apps Script**를 클릭합니다.
2. 기본으로 열려있는 `Code.gs`의 내용을 모두 지우고 아래 코드를 붙여넣습니다.

```javascript
const SHEET_NAME = '시험신청';
const HEADER = ['접수번호', '접수일시', '상태', '지파', '교회', '부서', '이름', '고유번호', '전화번호', '과목', '급수', '취소일시', '취소사유'];
const VISIT_SHEET_NAME = '방문기록';
const VISIT_HEADER = ['날짜', '방문시각'];
const SUGGEST_SHEET_NAME = '건의사항';
const SUGGEST_HEADER = ['접수일시', '이름', '건의내용'];

function getSheet_() {
  const ss = SpreadsheetApp.getActiveSpreadsheet();
  let sheet = ss.getSheetByName(SHEET_NAME);
  if (!sheet) {
    sheet = ss.insertSheet(SHEET_NAME);
  }
  if (sheet.getLastRow() === 0) {
    sheet.appendRow(HEADER);
  }
  return sheet;
}

function getVisitSheet_() {
  const ss = SpreadsheetApp.getActiveSpreadsheet();
  let sheet = ss.getSheetByName(VISIT_SHEET_NAME);
  if (!sheet) sheet = ss.insertSheet(VISIT_SHEET_NAME);
  if (sheet.getLastRow() === 0) sheet.appendRow(VISIT_HEADER);
  return sheet;
}

function getSuggestSheet_() {
  const ss = SpreadsheetApp.getActiveSpreadsheet();
  let sheet = ss.getSheetByName(SUGGEST_SHEET_NAME);
  if (!sheet) sheet = ss.insertSheet(SUGGEST_SHEET_NAME);
  if (sheet.getLastRow() === 0) sheet.appendRow(SUGGEST_HEADER);
  return sheet;
}

function jsonOut_(obj) {
  return ContentService.createTextOutput(JSON.stringify(obj)).setMimeType(ContentService.MimeType.JSON);
}

function pad2_(n) { return String(n).length < 2 ? '0' + n : String(n); }

// 접수번호를 YYMM-00001 형식으로 서버(시트)가 최종 부여한다.
// 같은 연월(YYMM) 안에서 이미 발급된 개수를 세어 다음 번호를 매긴다.
function nextApplyId_(sheet) {
  const now = new Date();
  const ym = String(now.getFullYear()).slice(2) + pad2_(now.getMonth() + 1);
  const data = sheet.getDataRange().getValues();
  let count = 0;
  for (let i = 1; i < data.length; i++) {
    if (String(data[i][0]).indexOf(ym + '-') === 0) count++;
  }
  return `${ym}-${String(count + 1).padStart(5, '0')}`;
}

function doPost(e) {
  const lock = LockService.getScriptLock();
  lock.waitLock(10000);
  try {
    const body = JSON.parse(e.postData.contents);
    const sheet = getSheet_();
    const action = body.action;

    if (action === 'submit') {
      const id = nextApplyId_(sheet);
      sheet.appendRow([
        id, body.appliedAt, body.status || '접수완료',
        body.tribe, body.church, body.dept,
        body.name, body.uid, body.phone,
        body.subject, body.grade, '', ''
      ]);
      return jsonOut_({ ok: true, id: id });
    }

    if (action === 'lookup') {
      const data = sheet.getDataRange().getValues();
      const rows = [];
      for (let i = 1; i < data.length; i++) {
        const r = data[i];
        if (String(r[6]) === body.name && String(r[7]) === body.uid) {
          rows.push({
            id: r[0], appliedAt: r[1], status: r[2],
            tribe: r[3], church: r[4], dept: r[5],
            name: r[6], uid: r[7], phone: r[8],
            subject: r[9], grade: r[10], cancelledAt: r[11], cancelReason: r[12]
          });
        }
      }
      return jsonOut_({ ok: true, rows: rows });
    }

    if (action === 'cancel') {
      const data = sheet.getDataRange().getValues();
      for (let i = 1; i < data.length; i++) {
        if (String(data[i][0]) === body.id) {
          sheet.getRange(i + 1, 3).setValue('취소');            // 상태
          sheet.getRange(i + 1, 12).setValue(body.cancelledAt); // 취소일시
          sheet.getRange(i + 1, 13).setValue(body.cancelReason || ''); // 취소사유
          return jsonOut_({ ok: true });
        }
      }
      return jsonOut_({ ok: false, error: 'not_found' });
    }

    if (action === 'visit') {
      const vSheet = getVisitSheet_();
      vSheet.appendRow([body.date, body.visitedAt || new Date()]);
      const data = vSheet.getDataRange().getValues();
      let count = 0;
      for (let i = 1; i < data.length; i++) {
        if (String(data[i][0]) === body.date) count++;
      }
      return jsonOut_({ ok: true, count: count });
    }

    if (action === 'suggest') {
      const sSheet = getSuggestSheet_();
      sSheet.appendRow([body.submittedAt, body.name || '익명', body.content || '']);
      return jsonOut_({ ok: true });
    }

    return jsonOut_({ ok: false, error: 'unknown_action' });
  } catch (err) {
    return jsonOut_({ ok: false, error: String(err) });
  } finally {
    lock.releaseLock();
  }
}

function doGet(e) {
  return jsonOut_({ ok: true, msg: '시온 공부방 시험 신청 API' });
}
```

3. 저장(디스크 아이콘 또는 Ctrl+S)합니다.

## 3. 웹앱으로 배포하기
1. Apps Script 화면 오른쪽 위 **배포 → 새 배포**를 클릭합니다.
2. 유형 선택(톱니바퀴)에서 **웹 앱**을 선택합니다.
3. 설정:
   - **실행 계정**: 나(본인)
   - **액세스 권한이 있는 사용자**: 전체
4. **배포**를 클릭하고, 처음이면 권한 승인 화면에서 본인 계정으로 승인합니다.
5. 배포가 끝나면 나오는 **웹 앱 URL**(`https://script.google.com/macros/s/xxxx/exec` 형태)을 복사합니다.

> 이후 시트 코드를 수정하면 "배포 → 배포 관리 → 수정(연필 아이콘) → 새 버전"으로 다시 배포해야 변경사항이 반영됩니다. URL은 그대로 유지됩니다.

## 4. index.html에 URL 붙여넣기
`index.html` 안에서 아래 줄을 찾아 큰따옴표 사이에 3번에서 복사한 URL을 붙여넣어 주세요.

```javascript
const GAS_URL = '';
```

예:
```javascript
const GAS_URL = 'https://script.google.com/macros/s/AKfycb.../exec';
```

이 값만 채워 넣으면 별도 코드 수정 없이 바로 구글 시트 연동이 활성화됩니다. URL을 알려주시면 제가 대신 붙여넣어 드릴게요.

## 참고
- 접수번호 형식은 `YYMM-00001`입니다(연도 2자리+월 2자리, 그 달 안에서의 5자리 일련번호). 시트가 연동되면 시트가 최종 번호를 부여하고, 연동 전에는 이 기기에서 임시로 같은 형식의 번호를 매깁니다.
- 신청서 제출/취소/조회는 모두 이 하나의 웹앱 URL로 처리됩니다.
- 신청 데이터는 시트에도 저장되고, 오프라인이거나 URL이 비어있을 때를 대비해 이 기기(브라우저)에도 백업됩니다.
- 이름 + 고유번호가 정확히 일치해야 취소/조회가 가능합니다(개인정보 보호 목적의 최소한의 확인 절차).
- 나중에 만들 대시보드는 이 시트를 그대로 데이터 소스로 읽으면 됩니다(Google Sheets API 또는 같은 웹앱에 `action: 'list'` 같은 조회용 엔드포인트를 추가하는 방식 모두 가능).

## 5. 오늘 방문자 수 / 개선 사항 건의 (같은 시트에 탭만 추가)
- 위 2번 코드에는 기존 `시험신청` 탭과 함께 `방문기록`, `건의사항` 탭을 자동으로 만드는 코드가 이미 포함되어 있습니다. **이미 배포된 웹앱이 있다면, 위 2번 코드를 최신 버전으로 다시 붙여넣고 "배포 → 배포 관리 → 수정(연필 아이콘) → 새 버전"으로 재배포해야 이 기능이 동작합니다.** (URL은 그대로 유지됩니다.)
- `방문기록` 탭: 홈(방 선택) 화면에 처음 들어올 때마다(같은 브라우저 탭에서는 하루 한 번만) 방문 기록이 한 줄씩 쌓이고, 화면에는 오늘 날짜에 해당하는 줄 수를 세어 "오늘 방문자 N명"으로 보여줍니다.
- `건의사항` 탭: 방 선택 화면의 "💡 개선 건의하기" 버튼으로 들어가 이름(선택)과 내용을 적어 제출하면 이 탭에 한 줄씩 쌓입니다.
- 둘 다 `GAS_URL`이 비어있거나 오프라인이면 이 기기(브라우저)에만 임시 저장되고, 배지에는 "(이 기기 기준)"이라고 표시됩니다.
