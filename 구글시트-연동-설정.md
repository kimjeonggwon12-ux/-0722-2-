# 시험 신청 → 구글 스프레드시트 연동 설정 방법

`index.html`의 "시험 신청" 화면은 이미 완성되어 있고, 지금은 신청 내역이 이 브라우저(기기)의 로컬 저장소에만 임시 저장됩니다.
아래 순서대로 진행하면 신청/취소 내역이 구글 스프레드시트에 실시간으로 쌓이고, 나중에 대시보드로 이어서 볼 수 있습니다.

## 1. 구글 스프레드시트 만들기
1. 새 구글 스프레드시트를 하나 만듭니다. (이름 예: `시온 공부방 시험 신청`)
2. 시트 탭 이름은 그대로 두거나 원하는 이름으로 바꿔도 됩니다. 단, 아래 2번 코드의 `SHEET_NAME` 값과 일치시켜 주세요. (기본값: `신청명단`)
3. `방문기록`, `건의사항`, `커뮤니티` 탭은 코드가 처음 호출될 때 자동으로 만들어지므로 미리 만들어두지 않아도 됩니다(이미 있다면 그대로 재사용합니다).

## 2. Apps Script 붙여넣기
1. 스프레드시트 메뉴에서 **확장 프로그램 → Apps Script**를 클릭합니다.
2. 기본으로 열려있는 `Code.gs`의 내용을 모두 지우고 아래 코드를 붙여넣습니다.

```javascript
const SHEET_NAME = '신청명단';
const HEADER = ['접수번호', '접수일시', '상태', '지파', '교회', '부서', '직책', '이름', '고유번호', '전화번호', '시험시간', '과목', '급수', '취소일시', '취소사유'];
const VISIT_SHEET_NAME = '방문기록';
const VISIT_HEADER = ['날짜', '방문시각'];
const SUGGEST_SHEET_NAME = '건의사항';
const SUGGEST_HEADER = ['접수일시', '이름', '교회', '부서', '전화번호', '건의내용', '조치완료', '답변완료'];
const COMMUNITY_SHEET_NAME = '커뮤니티';
// 수정토큰(F열)은 글/댓글 작성 시 서버가 발급해서 작성자에게만(communityPost 응답으로) 돌려주고,
// communityList 응답에는 절대 포함하지 않는다 — 이 값을 아는 사람만 그 글/댓글을 수정·삭제할 수
// 있으므로, 로그인 없이도 "내가 쓴 것만 고칠 수 있음"이 성립한다.
// 고정여부(G열)는 관리자(ADMIN_KEY)만 communityPin 액션으로 바꿀 수 있고, 상위 글(댓글이 아닌
// 것)에만 의미가 있다. 좋아요수(H열)는 communityLike 액션으로 누구나 +1/-1 할 수 있다 —
// 로그인이 없어 "이 사람이 이미 눌렀는지"는 서버가 검증하지 못하고, 클라이언트(이 브라우저의
// localStorage)만 스스로 중복을 막는다.
const COMMUNITY_HEADER = ['글번호', '작성일시', '이름', '내용', '상위글번호', '수정토큰', '고정여부', '좋아요수'];
// 건의사항 대시보드(목록 조회/체크 표시) 접근용 별도 비밀번호. 원하는 값으로 바꿔서 쓰세요.
const ADMIN_KEY = 'sion-admin-2026';

function getSheet_() {
  const ss = SpreadsheetApp.getActiveSpreadsheet();
  let sheet = ss.getSheetByName(SHEET_NAME);
  if (!sheet) sheet = ss.insertSheet(SHEET_NAME);
  if (sheet.getLastRow() === 0) sheet.appendRow(HEADER);
  // 고유번호(I열)·전화번호(J열)는 앞자리 0이 지워지지 않도록 항상 텍스트 서식으로 고정한다
  sheet.getRange('I:I').setNumberFormat('@');
  sheet.getRange('J:J').setNumberFormat('@');
  return sheet;
}

function getVisitSheet_() {
  const ss = SpreadsheetApp.getActiveSpreadsheet();
  let sheet = ss.getSheetByName(VISIT_SHEET_NAME);
  if (!sheet) sheet = ss.insertSheet(VISIT_SHEET_NAME);
  if (sheet.getLastRow() === 0) sheet.appendRow(VISIT_HEADER);
  return sheet;
}

function getSuggestSheet_() {
  const ss = SpreadsheetApp.getActiveSpreadsheet();
  let sheet = ss.getSheetByName(SUGGEST_SHEET_NAME);
  if (!sheet) sheet = ss.insertSheet(SUGGEST_SHEET_NAME);
  if (sheet.getLastRow() === 0) sheet.appendRow(SUGGEST_HEADER);
  return sheet;
}

function getCommunitySheet_() {
  const ss = SpreadsheetApp.getActiveSpreadsheet();
  let sheet = ss.getSheetByName(COMMUNITY_SHEET_NAME);
  if (!sheet) sheet = ss.insertSheet(COMMUNITY_SHEET_NAME);
  if (sheet.getLastRow() === 0) sheet.appendRow(COMMUNITY_HEADER);
  return sheet;
}

function jsonOut_(obj) {
  return ContentService.createTextOutput(JSON.stringify(obj)).setMimeType(ContentService.MimeType.JSON);
}

function pad2_(n) { return String(n).length < 2 ? '0' + n : String(n); }

// 고유번호는 시트에 앞자리 0이 지워진 채로 남아있는 과거 행이 섞여 있을 수 있어
// 비교할 때는 앞자리 0을 무시하고 맞춰본다 (예: "0012345678901" == "12345678901")
function normId_(v) {
  const s = String(v == null ? '' : v).trim().replace(/^0+/, '');
  return s === '' ? '0' : s;
}
function normName_(v) { return String(v == null ? '' : v).trim(); }

// 접수번호를 YYMM-00001 형식으로 서버(시트)가 최종 부여한다.
// 같은 연월(YYMM) 안에서 이미 발급된 개수를 세어 다음 번호를 매긴다.
function nextApplyId_(sheet) {
  const now = new Date();
  const ym = String(now.getFullYear()).slice(2) + pad2_(now.getMonth() + 1);
  const data = sheet.getDataRange().getValues();
  let count = 0;
  for (let i = 1; i < data.length; i++) {
    if (String(data[i][0]).indexOf(ym + '-') === 0) count++;
  }
  return `${ym}-${String(count + 1).padStart(5, '0')}`;
}

function doPost(e) {
  const lock = LockService.getScriptLock();
  lock.waitLock(10000);
  try {
    const body = JSON.parse(e.postData.contents);
    const action = body.action;

    if (action === 'apply') {
      const sheet = getSheet_();
      // 같은 이름 + 같은 고유번호로 아직 취소하지 않은 신청이 이미 있으면 중복 신청을 막는다
      const existing = sheet.getDataRange().getValues();
      for (let i = 1; i < existing.length; i++) {
        const r = existing[i];
        if (normName_(r[7]) === normName_(body.name) && normId_(r[8]) === normId_(body.memberId) && r[2] !== '취소') {
          return jsonOut_({ success: false, message: '이미 신청하셨습니다. 이전 신청을 취소하고 다시 신청해주세요.' });
        }
      }
      const id = nextApplyId_(sheet);
      const first = (body.subjects && body.subjects[0]) || {};
      sheet.appendRow([
        id, body.appliedAt || new Date(), '접수완료',
        body.tribe || '', body.church || '', body.department || '', body.position || '',
        body.name || '', body.memberId || '', body.phone || '', body.examTime || '',
        first.subject || '', first.grade || '', '', ''
      ]);
      return jsonOut_({ success: true, message: '신청이 접수되었습니다.', id: id });
    }

    if (action === 'lookup') {
      const sheet = getSheet_();
      const data = sheet.getDataRange().getValues();
      const rows = [];
      // 고유번호는 선택 사항 — 비어있으면 이름만으로 찾는다(동명이인이면 여러 건이 반환되고,
      // 클라이언트가 memberId로 구분해 선택하게 한다). 고유번호를 입력했으면 기존처럼 정확히 일치해야 한다.
      const hasMemberId = body.memberId != null && String(body.memberId).trim() !== '';
      for (let i = 1; i < data.length; i++) {
        const r = data[i];
        if (normName_(r[7]) === normName_(body.name) && (!hasMemberId || normId_(r[8]) === normId_(body.memberId))) {
          rows.push({
            id: r[0], appliedAt: r[1], status: r[2],
            tribe: r[3], church: r[4], department: r[5], position: r[6],
            name: r[7], memberId: r[8], phone: r[9], examTime: r[10],
            subject: r[11], grade: r[12], cancelledAt: r[13], reason: r[14]
          });
        }
      }
      return jsonOut_({ success: true, rows: rows });
    }

    if (action === 'cancel') {
      const sheet = getSheet_();
      const data = sheet.getDataRange().getValues();
      // 이름+고유번호가 일치하는 신청 중 아직 취소되지 않은 가장 최근 건을 취소한다
      // (신청 화면이 "중복 신청 불가" 정책이라 보통 활성 신청은 최대 1건입니다)
      for (let i = data.length - 1; i >= 1; i--) {
        const r = data[i];
        if (normName_(r[7]) === normName_(body.name) && normId_(r[8]) === normId_(body.memberId) && r[2] !== '취소') {
          sheet.getRange(i + 1, 3).setValue('취소');            // 상태
          sheet.getRange(i + 1, 14).setValue(new Date());       // 취소일시
          sheet.getRange(i + 1, 15).setValue(body.reason || ''); // 취소사유
          return jsonOut_({ success: true, message: '취소되었습니다.' });
        }
      }
      return jsonOut_({ success: false, message: '취소할 신청 내역을 찾을 수 없습니다.' });
    }

    // 신청 변경: 취소 후 재신청 대신, 기존 신청 건을 그 자리에서 수정한다.
    // 접수번호(id) + 이름 + 고유번호가 모두 일치하는 행만 수정 대상으로 삼고,
    // 접수번호·접수일시·상태·이름·고유번호·취소일시·취소사유는 건드리지 않는다.
    if (action === 'update') {
      const sheet = getSheet_();
      const data = sheet.getDataRange().getValues();
      for (let i = 1; i < data.length; i++) {
        const r = data[i];
        if (String(r[0]) === String(body.id) && normName_(r[7]) === normName_(body.name) && normId_(r[8]) === normId_(body.memberId)) {
          if (r[2] === '취소') {
            return jsonOut_({ success: false, message: '이미 취소된 신청은 변경할 수 없습니다. 새로 신청해주세요.' });
          }
          const first = (body.subjects && body.subjects[0]) || {};
          sheet.getRange(i + 1, 4).setValue(body.tribe || '');       // 지파
          sheet.getRange(i + 1, 5).setValue(body.church || '');      // 교회
          sheet.getRange(i + 1, 6).setValue(body.department || '');  // 부서
          sheet.getRange(i + 1, 7).setValue(body.position || '');    // 직책
          sheet.getRange(i + 1, 10).setValue(body.phone || '');      // 전화번호
          sheet.getRange(i + 1, 11).setValue(body.examTime || '');   // 시험시간
          sheet.getRange(i + 1, 12).setValue(first.subject || '');   // 과목
          sheet.getRange(i + 1, 13).setValue(first.grade || '');     // 급수
          return jsonOut_({ success: true, message: '신청이 변경되었습니다.' });
        }
      }
      return jsonOut_({ success: false, message: '변경할 신청 내역을 찾을 수 없습니다.' });
    }

    if (action === 'visit') {
      const vSheet = getVisitSheet_();
      vSheet.appendRow([body.date, body.visitedAt || new Date()]);
      const data = vSheet.getDataRange().getValues();
      let count = 0;
      for (let i = 1; i < data.length; i++) {
        if (String(data[i][0]) === body.date) count++;
      }
      return jsonOut_({ success: true, count: count });
    }

    if (action === 'suggest') {
      const sSheet = getSuggestSheet_();
      sSheet.appendRow([
        body.submittedAt || new Date(), body.name || '익명',
        body.church || '', body.department || '', body.phone || '', body.content || '',
        false, false
      ]);
      return jsonOut_({ success: true, message: '건의사항이 접수되었습니다.' });
    }

    // 건의사항 대시보드: 목록 조회 (관리자 비밀번호 필요)
    if (action === 'listSuggestions') {
      if (body.adminKey !== ADMIN_KEY) return jsonOut_({ success: false, message: '권한이 없습니다.' });
      const sSheet = getSuggestSheet_();
      const data = sSheet.getDataRange().getValues();
      const rows = [];
      for (let i = 1; i < data.length; i++) {
        const r = data[i];
        rows.push({
          row: i + 1, // 시트상 실제 행 번호(1-based, 1행은 헤더) - 상태 업데이트 시 이 값으로 행을 찾음
          submittedAt: r[0], name: r[1], church: r[2], department: r[3],
          phone: r[4], content: r[5],
          resolved: r[6] === true || r[6] === 'TRUE',
          answered: r[7] === true || r[7] === 'TRUE'
        });
      }
      return jsonOut_({ success: true, rows: rows });
    }

    // 건의사항 대시보드: 조치완료/답변완료 체크 상태 저장 (관리자 비밀번호 필요)
    if (action === 'updateSuggestionStatus') {
      if (body.adminKey !== ADMIN_KEY) return jsonOut_({ success: false, message: '권한이 없습니다.' });
      const row = Number(body.row);
      const col = body.field === 'resolved' ? 7 : body.field === 'answered' ? 8 : 0;
      if (!row || row < 2 || !col) return jsonOut_({ success: false, message: '잘못된 요청입니다.' });
      const sSheet = getSuggestSheet_();
      sSheet.getRange(row, col).setValue(!!body.value);
      return jsonOut_({ success: true });
    }

    // 커뮤니티: 글/댓글 작성. parentId가 비어있으면 새 글, 채워져 있으면 그 글의 댓글이다.
    // 관리자 비밀번호 없이 누구나 호출할 수 있다(앱 자체가 이미 입장 비밀번호로 보호돼 있음).
    // 수정토큰(editToken)을 새로 발급해 응답에만 실어 보낸다 — 이 값을 가진 클라이언트만 나중에
    // 이 글/댓글을 수정·삭제할 수 있다.
    if (action === 'communityPost') {
      const content = String(body.content || '').trim();
      if (!content) return jsonOut_({ success: false, message: '내용을 입력해주세요.' });
      const cSheet = getCommunitySheet_();
      const id = Utilities.getUuid();
      const editToken = Utilities.getUuid();
      cSheet.appendRow([id, body.createdAt || new Date(), body.name || '익명', content, body.parentId || '', editToken, false, 0]);
      return jsonOut_({ success: true, id: id, editToken: editToken });
    }

    // 커뮤니티: 전체 글+댓글 목록 조회(관리자 비밀번호 불필요, 누구나 볼 수 있음)
    // 수정토큰(F열)은 절대 응답에 포함하지 않는다 — 작성자 본인 브라우저에만 남아있어야 한다.
    if (action === 'communityList') {
      const cSheet = getCommunitySheet_();
      const data = cSheet.getDataRange().getValues();
      const rows = [];
      for (let i = 1; i < data.length; i++) {
        const r = data[i];
        if (!r[0]) continue;
        rows.push({
          id: String(r[0]), createdAt: r[1], name: r[2], content: r[3], parentId: r[4] ? String(r[4]) : '',
          pinned: r[6] === true || r[6] === 'TRUE',
          likes: Number(r[7]) || 0
        });
      }
      return jsonOut_({ success: true, rows: rows });
    }

    // 커뮤니티: 글/댓글 수정. 작성 시 발급받은 수정토큰이 그 행의 F열 값과 정확히 일치할 때만 허용한다.
    if (action === 'communityEdit') {
      const content = String(body.content || '').trim();
      if (!content) return jsonOut_({ success: false, message: '내용을 입력해주세요.' });
      const cSheet = getCommunitySheet_();
      const data = cSheet.getDataRange().getValues();
      for (let i = 1; i < data.length; i++) {
        const r = data[i];
        if (String(r[0]) === String(body.id)) {
          if (!body.editToken || String(r[5]) !== String(body.editToken)) {
            return jsonOut_({ success: false, message: '수정 권한이 없습니다.' });
          }
          cSheet.getRange(i + 1, 4).setValue(content);
          return jsonOut_({ success: true });
        }
      }
      return jsonOut_({ success: false, message: '글을 찾을 수 없습니다.' });
    }

    // 커뮤니티: 글/댓글 삭제. 글(댓글이 아닌 것)을 지우면 그 글에 달린 댓글도 함께 지운다.
    if (action === 'communityDelete') {
      const cSheet = getCommunitySheet_();
      const data = cSheet.getDataRange().getValues();
      let targetRow = -1;
      for (let i = 1; i < data.length; i++) {
        if (String(data[i][0]) === String(body.id)) { targetRow = i; break; }
      }
      if (targetRow === -1) return jsonOut_({ success: false, message: '글을 찾을 수 없습니다.' });
      if (!body.editToken || String(data[targetRow][5]) !== String(body.editToken)) {
        return jsonOut_({ success: false, message: '삭제 권한이 없습니다.' });
      }
      // 삭제 대상 행 + 그 글에 달린 댓글 행들을 모아서, 뒤(아래)쪽 행부터 지운다
      // (앞쪽부터 지우면 뒤 행들의 실제 행 번호가 하나씩 밀려서 꼬인다).
      const rowsToDelete = [targetRow + 1];
      for (let i = 1; i < data.length; i++) {
        if (String(data[i][4]) === String(body.id)) rowsToDelete.push(i + 1);
      }
      rowsToDelete.sort((a, b) => b - a).forEach(r => cSheet.deleteRow(r));
      return jsonOut_({ success: true });
    }

    // 커뮤니티: 글 고정/고정 해제 (관리자 비밀번호 필요). 댓글에는 의미가 없으므로 클라이언트가
    // 상위 글에만 이 액션을 호출하도록 하고 있지만, 서버는 어느 행이든(글/댓글 구분 없이) 요청된
    // id의 G열 값만 바꾼다.
    if (action === 'communityPin') {
      if (body.adminKey !== ADMIN_KEY) return jsonOut_({ success: false, message: '권한이 없습니다.' });
      const cSheet = getCommunitySheet_();
      const data = cSheet.getDataRange().getValues();
      for (let i = 1; i < data.length; i++) {
        if (String(data[i][0]) === String(body.id)) {
          cSheet.getRange(i + 1, 7).setValue(!!body.pinned);
          return jsonOut_({ success: true });
        }
      }
      return jsonOut_({ success: false, message: '글을 찾을 수 없습니다.' });
    }

    // 커뮤니티: 좋아요 +1/-1 (관리자 비밀번호 불필요, 글/댓글 둘 다 가능). liked:true면 +1,
    // false면 -1(0 밑으로는 안 내려감). 같은 사람이 여러 번 누르는 것을 서버가 막지는 못하고
    // (로그인이 없어 "같은 사람"을 알 수 없음), 클라이언트가 이 브라우저 기준으로만 중복을 막는다.
    if (action === 'communityLike') {
      const cSheet = getCommunitySheet_();
      const data = cSheet.getDataRange().getValues();
      for (let i = 1; i < data.length; i++) {
        if (String(data[i][0]) === String(body.id)) {
          const current = Number(data[i][7]) || 0;
          const next = Math.max(0, current + (body.liked ? 1 : -1));
          cSheet.getRange(i + 1, 8).setValue(next);
          return jsonOut_({ success: true, likes: next });
        }
      }
      return jsonOut_({ success: false, message: '글을 찾을 수 없습니다.' });
    }

    return jsonOut_({ success: false, message: 'unknown_action' });
  } catch (err) {
    return jsonOut_({ success: false, message: String(err) });
  } finally {
    lock.releaseLock();
  }
}

function doGet(e) {
  return jsonOut_({ success: true, message: '시온 공부방 시험 신청 API' });
}
```

3. 저장(디스크 아이콘 또는 Ctrl+S)합니다.

## 3. 웹앱으로 배포하기
1. Apps Script 화면 오른쪽 위 **배포 → 새 배포**를 클릭합니다. (이미 배포되어 있다면 **배포 → 배포 관리 → 수정(연필 아이콘) → 새 버전**)
2. 유형 선택(톱니바퀴)에서 **웹 앱**을 선택합니다.
3. 설정:
   - **실행 계정**: 나(본인)
   - **액세스 권한이 있는 사용자**: 전체
4. **배포**를 클릭하고, 처음이면 권한 승인 화면에서 본인 계정으로 승인합니다.
5. 배포가 끝나면 나오는 **웹 앱 URL**(`https://script.google.com/macros/s/xxxx/exec` 형태)을 복사합니다. (이미 URL이 있다면 그대로 유지됩니다 — "새 버전"으로 재배포한 경우 URL은 바뀌지 않습니다.)

## 4. index.html에 URL 붙여넣기
`index.html` 안에서 아래 줄을 찾아 큰따옴표 사이에 3번에서 복사한 URL을 붙여넣어 주세요.

```javascript
const GAS_URL = '';
```

예:
```javascript
const GAS_URL = 'https://script.google.com/macros/s/AKfycb.../exec';
```

이 값만 채워 넣으면 별도 코드 수정 없이 바로 구글 시트 연동이 활성화됩니다. URL을 알려주시면 제가 대신 붙여넣어 드릴게요.

## 참고
- 접수번호 형식은 `YYMM-00001`입니다(연도 2자리+월 2자리, 그 달 안에서의 5자리 일련번호). 시트가 최종 번호를 부여합니다.
- 신청서 제출/취소/변경/조회는 모두 이 하나의 웹앱 URL로 처리됩니다. 응답은 항상 `{ success: true/false, message: '...' }` 형태이고, 조회(`lookup`)는 여기에 `rows` 배열이 추가됩니다.
- 신청 데이터는 시트에도 저장되고, 오프라인이거나 URL이 비어있을 때를 대비해 이 기기(브라우저)에도 백업됩니다.
- 조회(`lookup`)는 이름만으로도 가능합니다. 동명이인이 있으면 여러 건이 반환되고, 앱 화면에서 지파·교회·부서·직책 등 기본 정보를 보여준 뒤 본인을 선택하게 합니다. 고유번호까지 입력하면 그 사람만 정확히 찾습니다. 실제로 신청을 취소하거나 변경할 때는(뒤에서 어떤 사람을 선택했든) 항상 이름+고유번호가 정확히 일치해야 처리됩니다(개인정보 보호 목적의 최소한의 확인 절차).
- 나중에 만들 대시보드는 이 시트를 그대로 데이터 소스로 읽으면 됩니다.

## 5. 방문 기록 / 개선 사항 건의 / 커뮤니티 (같은 시트에 탭만 추가)
- 위 2번 코드에는 `신청명단` 탭과 함께 `방문기록`, `건의사항`, `커뮤니티` 탭을 자동으로 만드는 코드가 포함되어 있습니다. 이미 탭이 스프레드시트에 있다면 그대로 재사용합니다(새로 만들지 않음).
- `방문기록` 탭: 홈(방 선택) 화면에 들어올 때마다(로그인 성공 직후, 다른 화면에서 "홈으로" 돌아올 때 등 매번) `{ action: 'visit', date, visitedAt }`을 전송해 한 줄씩 쌓입니다. 세션당 1회 제한 없이 들어올 때마다 매번 보냅니다. 응답을 기다리지 않는 fire-and-forget 방식이라 전송 성공/실패가 화면 동작에 전혀 영향을 주지 않고, 실패하면 콘솔에만 로그가 남습니다. 화면에 방문자 수를 보여주는 배지는 없으므로, 집계가 필요하면 시트를 직접 열어서 확인하세요. `GAS_URL`이 비어있으면 이 요청 자체를 보내지 않습니다(로컬 백업 없음).
- `건의사항` 탭: 방 선택 화면의 "💡 개선 건의하기" 버튼으로 들어가 이름(선택)·교회(필수)·부서(필수)·전화번호(선택)·내용을 적어 제출하면 이 탭에 한 줄씩 쌓입니다. `GAS_URL`이 비어있거나 오프라인이면 이 기기(브라우저)에만 임시 저장됩니다.
- `커뮤니티` 탭: 방 선택 화면의 "🤗 커뮤니티" 버튼(빨간 NEW 뱃지)으로 들어가 누구나 이름(선택, 비우면 익명)과 내용만 적어 글/댓글을 남길 수 있습니다. 로그인·가입 절차가 따로 없고, 앱에 이미 들어와 있다는 것 자체가 참여 자격입니다. 글과 댓글이 같은 탭에 한 행씩 쌓이며 `상위글번호`가 비어있으면 글, 채워져 있으면 그 글번호의 댓글입니다. 다른 사람이 쓴 글도 유튜브 댓글처럼 목록으로 보이고(답글은 접지 않고 기본으로 바로 펼쳐서 보여줌), 화면이 떠 있는 동안 25초마다 자동으로 다시 불러와 새 글/댓글이 자연스럽게 반영됩니다. 자기가 쓴 글/댓글만 수정·삭제할 수 있는데, 로그인이 없으므로 작성 시 서버가 발급하는 1회용 수정토큰(F열, `communityList` 응답에는 절대 포함 안 됨)을 작성자 브라우저의 localStorage에만 저장해뒀다가 수정·삭제 요청에 같이 실어 보내 확인하는 방식입니다(다른 관리 기능처럼 `ADMIN_KEY`를 쓰지 않음). 그 밖의 악성 게시물 방지용 필터링은 없으므로, 문제가 생기면 시트에서 해당 행을 직접 지우면 됩니다. `GAS_URL`이 비어있으면 커뮤니티 화면에 "지금은 사용할 수 없다"는 안내만 뜨고 로컬 저장 없이 그냥 막힙니다(다른 사람에게 안 보이는 글을 쓴 것처럼 착각하지 않도록 일부러 로컬 백업을 하지 않았습니다). 글/댓글마다 좋아요(❤️)를 누를 수 있고 누른 즉시 숫자가 바뀝니다(H열 `좋아요수`, `communityLike` 액션, +1/-1만 하는 단순한 카운터라 관리자 비밀번호 불필요) — 로그인이 없어 "같은 사람이 두 번 눌렀는지"는 서버가 구분 못 하고, 이 브라우저 기준으로만(localStorage) 중복을 막습니다. 목록 정렬은 ①관리자가 고정한 글(항상 맨 위) → ②고정 안 된 글 중 좋아요·답글이 많은 "인기" 글 최대 10개(좋아요 수 우선, 동률이면 답글 수) → ③나머지는 최신순입니다. 글 고정/해제는 커뮤니티 화면의 "🔑 관리자" 버튼으로 비밀번호(건의사항 대시보드와 같은 `ADMIN_KEY`)를 넣으면 각 글에 "📌 고정" 버튼이 나타나는 방식입니다(G열 `고정여부`, `communityPin` 액션).

## 6. 건의사항 관리자 대시보드
- `index.html`을 `#admin-suggest` 해시를 붙여서 열면(예: `index.html#admin-suggest`) 관리자 전용 대시보드가 뜹니다. 방 선택 화면 등 앱 UI 어디에도 이 화면으로 가는 버튼/링크는 없고, 이 주소를 직접 입력하거나 즐겨찾기해서만 들어갈 수 있습니다.
- 처음 들어가면 비밀번호를 물어보는데, 이 값은 2번 코드의 `ADMIN_KEY` 상수와 같아야 합니다(기본값 `sion-admin-2026` — 실제 배포 전에 원하는 값으로 바꾸는 것을 권장합니다. 앱의 일반 입장 암호와는 별개의 값입니다).
- 대시보드에는 접수된 건의사항이 최신순으로 표(접수일시·이름·교회·부서·전화번호·건의내용)로 나오고, 맨 오른쪽에 **조치 완료 / 답변 완료** 체크박스가 있습니다. 체크하면 즉시 시트의 해당 행(`조치완료`/`답변완료` 열)에 저장되어, 이후 누가 다시 열어도(다른 기기 포함) 같은 체크 상태로 보입니다.
- 이 기능은 2번 코드의 `listSuggestions`/`updateSuggestionStatus` 액션을 사용하므로, 새로 붙여넣은 코드로 **반드시 "새 버전" 재배포**를 해야 동작합니다(붙여넣기만 하고 재배포를 안 하면 이전 버전 코드로 계속 응답합니다).

## 7. 신청 변경(update) 기능

- 취소 후 재신청 대신, 이미 낸 신청 건을 그 자리에서 바로 수정할 수 있는 기능입니다. "신청 조회 · 신청 취소" 화면에서 조회한 뒤, 아직 취소되지 않은 신청 건에 있는 "✏️ 신청 변경" 버튼으로 들어갑니다.
- 지파·교회·부서·직책·전화번호·시험시간·과목·급수를 바꿀 수 있고, 이름·고유번호(식별키)와 접수번호·접수일시·상태는 바뀌지 않습니다.
- 이 기능은 2번 코드의 `update` 액션을 사용하므로, 새로 붙여넣은 코드로 **반드시 "새 버전" 재배포**를 해야 동작합니다. 재배포하지 않으면 "변경할 신청 내역을 찾을 수 없습니다" 같은 오류가 계속 나거나, 이전 코드가 `update`를 몰라 `unknown_action`으로 응답합니다.
